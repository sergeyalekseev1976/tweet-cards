                  Tweets Cards

This project contains two main pages: the home page and the tweets page. The home page displays a greeting and the tweets page displays the user's tweets.
It allows users to subscribe or unsubscribe from each other. The app uses React, React Router, Redux, Axios and Styled Components .